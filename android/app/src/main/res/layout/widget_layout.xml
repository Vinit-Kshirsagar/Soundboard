<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <!-- Top Icon/Logo -->
    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:src="@drawable/ic_widget_sound"
        android:contentDescription="Sound Icon"
        android:tint="#FFFFFF" />

    <!-- Sound Name -->
    <TextView
        android:id="@+id/widget_sound_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_icon"
        android:layout_marginTop="12dp"
        android:text="No Sound Selected"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_sound_name"
        android:layout_marginTop="4dp"
        android:text="Tap to play"
        android:textColor="#CCCCCC"
        android:textSize="12sp"
        android:gravity="center" />

</RelativeLayout>